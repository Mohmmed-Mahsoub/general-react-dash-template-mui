import styles from "./home.module.css";

const Home = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Home
    </div>
  );
};

export default Home;
