import styles from "./notFound.module.css";
const NotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      NotFound
    </div>
  );
};

export default NotFound;
